# Architecture

This project is structured as a full-stack AI meeting assistant with:
- a Next.js frontend for the user interface
- a FastAPI backend for API and AI processing
- a Docker-based deployment setup
